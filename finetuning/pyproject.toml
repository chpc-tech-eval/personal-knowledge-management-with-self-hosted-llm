[project]
name = "finetuning"
version = "0.1.0"
description = "TODO"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0.0",
    "transformers>=4.40.0",
    "datasets>=2.14.0",
    "accelerate>=0.20.0",
]
