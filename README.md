# Personal Knowledge Management with Self-Hosted LLM

Run with `uv run main.py`

Wait for the models to download and then start chatting ^-^

(Hopefully, I need someone else to test, especially with environment-mismatch issues.)

### How to install UV for your user

Run `curl -LsSf https://astral.sh/uv/install.sh | sh`
